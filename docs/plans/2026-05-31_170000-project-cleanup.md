# Plan: Clean Up & Archive Project Folder v0.4 → v0.5

## Goal
Rapikan folder `hermes-vtuber-host` dengan mengarsipkan file-file lama ke `archive/v0.x/` berdasarkan versi, sambil memastikan bridge (v0.4) masih bisa dijalankan.

## Current State
Folder project berantakan:
- 15+ file `test_*.py` berserakan di root
- 14+ file `session_logs/*.log` 
- File random: `31 Mei 2026_1644_artistream.txt`, `ARTI_MOOD_STATE.json`, `chat_relay.html`, `sync_exp3.sh`
- Folder `supertone_pitch_test/`, `supertone_voice_samples/`
- `__pycache__/` dua dari test file
- `vts_token.txt.bak`
- `.kiro/`, `.hypothesis/`, `.pytest_cache/`

## Struktur Target

```
hermes-vtuber-host/
├── hermes_vtuber_bridge.py          # MAIN — tetap di root
├── subtitle_server.py               # WebSocket subtitle server
├── subtitle.html                    # OBS Browser Source
├── mouse_follow.py                  # Mouse follow (WIP)
├── text_preprocessor.py             # Number-to-words
├── supertone_engine.py              # Supertone subprocess
├── requirements.txt                 # Python dependencies
├── requirements-supertone.txt       # Supertone dependencies
├── ARTI_SOUL.md                     # Arti personality
├── ARTI_VIEWERS.md                  # Viewer tracker
├── vts_token.txt                    # VTS auth token
├── templates/                       # exp3.json templates
├── vault/                           # Hermes Vault (sessions + concepts)
├── session_logs/                    # Current session logs (keep recent)
├── tests/                           # Semua test files ← NEW
│   ├── test_*.py
│   └── __pycache__/                 # Test pycache
├── archive/                         # Arsip versi lama ← NEW
│   ├── v0.1/                        # Baseline
│   ├── v0.3/                        # + expressions, plans
│   ├── v0.3.1/                      # + subtitle, supertone
│   └── v0.4/                        # + tests, voice samples, pitch test
└── .hermes/                         # Hermes config (keep)
```

## Step-by-Step Plan

### 1. Buat folder struktur baru
```
mkdir -p tests archive/v0.1 archive/v0.3 archive/v0.3.1 archive/v0.4
```

### 2. Arsipkan test files → `tests/`
Pindahkan SEMUA `test_*.py` dan `__pycache__/` test ke `tests/`:
- `test_fallback_integration.py`
- `test_protocol_roundtrip.py`
- `test_socialstream.py`
- `test_speak_fallback.py`
- `test_speak_totality_properties.py`
- `test_subtitle.py`
- `test_subtitle_shape_properties.py`
- `test_supertone_engine.py`
- `test_supertone_integration.py`
- `test_supertone_process_lifecycle.py`
- `test_supertone_process_properties.py`
- `test_text_preprocessor_edge_cases.py`
- `test_text_preprocessor_properties.py`
- `test_tts.py`
- `test_ytchat.py`
- `__pycache__/` (test-related .pyc files)

### 3. Arsipkan file random → `archive/v0.4/`
- `31 Mei 2026_1644_artistream.txt` (stream log manual)
- `ARTI_MOOD_STATE.json` (mood state — masih dipakai? cek dulu)
- `chat_relay.html` (HTML relay — masih dipakai?)
- `sync_exp3.sh` (sync script — masih dipakai?)
- `vts_token.txt.bak` (backup token)
- `.kiro/` (Kiro specs — arsip)
- `.hypothesis/` (Hypothesis cache — arsip)
- `.pytest_cache/` (pytest cache — arsip)

### 4. Arsipkan supertone development files → `archive/v0.4/`
- `supertone_pitch_test/` (folder)
- `supertone_pitch_test.py`
- `supertone_tts_test.py`
- `supertone_voice_samples/` (folder)
- `supertone_voice_guide.md`

### 5. Bersihkan session_logs
- Keep 2-3 log terbaru di `session_logs/`
- Pindahkan sisanya ke `archive/v0.4/session_logs/`

### 6. Verifikasi bridge masih jalan
- `python hermes_vtuber_bridge.py`
- Pastikan: VTS connect, ASR active, toggle jalan, subtitle server start
- Kalau error, cek import paths dan file references

### 7. Update .gitignore
Tambahkan:
```
__pycache__/
*.pyc
.vscode/
.idea/
```

### 8. Commit
```
git add -A
git commit -m "chore: clean up project folder — archive v0.1-v0.4, move tests to tests/"
```

## Files That Must NOT Be Moved
- `hermes_vtuber_bridge.py` — MAIN
- `subtitle_server.py` — WebSocket server
- `subtitle.html` — OBS Browser Source
- `mouse_follow.py` — WIP feature
- `text_preprocessor.py` — TTS preprocessor
- `supertone_engine.py` — Supertone subprocess
- `requirements.txt`, `requirements-supertone.txt` — dependencies
- `ARTI_SOUL.md`, `ARTI_VIEWERS.md` — config
- `vts_token.txt` — auth token
- `templates/` — exp3.json templates
- `vault/` — Hermes Vault
- `.hermes/` — Hermes config
- `.git/` — Git repo

## Risks
- Test files mungkin masih di-import oleh pytest — cek `conftest.py` atau pytest config
- `ARTI_MOOD_STATE.json` mungkin masih dibaca bridge — cek dulu sebelum dipindah
- `chat_relay.html` mungkin masih dipakai — cek dulu
- `sync_exp3.sh` mungkin masih dipakai — cek dulu

## Verification
Setelah cleanup:
1. `python hermes_vtuber_bridge.py` → harus jalan tanpa error
2. Toggle ON → bicara → Arti respon
3. Subtitle muncul di OBS
4. Idle animation jalan
5. `pytest tests/` → semua test harus pass
