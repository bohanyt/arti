# ARTI v0.7 — State lengkap pra-live (2026-08-02 sore)

> Dokumen anti-lupa pasca /compact + brief review subagent. Folder docs/handoff
> SENGAJA di luar index RAG Arti.

## Posisi

Branch `feat/cursor-brain-foundation`. Rollback: tag `v0.6.4-stable` +
`ARTI-backups/2026-08-02_v0.6.4-stable/`. Backup terbaru penuh (bundle+vault+DB):
`ARTI-backups/2026-08-02_v0.7-lengkap/`. Suite **468 lulus / 1 skip, RC=0**.
Tree bersih. Semua fitur v0.7 dibangun & ter-commit; SEBAGIAN belum live-verified.

## Commit sejak v0.6.4-stable (urut)

- `e4dd36c` chore: subtitle.html 9991 + komentar routing curious
- `b07405b` docs: pointer rollback v0.6.4 (dibuat SEBELUM tag — tag menyusul komit ini)
- `74fa874` feat(A): inisiatif topik saat hening (versi 1)
- `f2e8346` feat(B): recency RAG ([HARI INI] per-turn, decay half-life 14d, temporal ×2)
- `3ed0472` fix(B): larangan mengumumkan tanggal
- `6a4b09e` feat: nickname viewer pendek (viewer_nickname + jaring post_process)
- `3ff53ca` fix(A): presence via _yt_viewers_seen di add_to_history (dict lama khusus queue-mode)
- `6b50d33` feat(C): web lookup (groq/compound-mini 6,4-6,8s -> cursor lookup grok/low 17,6s)
- `9ef9d0e` test: kunci kebijakan HANYA composer-2.5 & grok-4.5(high), NOT FAST
- `d27f912` feat: rant mode + instruksi panjang selaras plan adaptif
- `ba91c43` fix: 4 temuan tes live (vision_turn_budget 15s; 'liat' polos bukan layar; strip CJK; muka eksplisit menang)
- `7d4236f` feat: shutdown tunggu tuntas + progress embedding + banner
- `d8cf11e` tune: vision budget/kapan/skip-curious (3 temuan live pagi)
- `df294f4` feat(A): SPEK FINAL hening — lihat bagian Inisiatif
- `1ae5220` feat(D0): Super Chat = donation tanpa wake word
- `bae08ed` feat(D0): reaksi donasi tunggu alert overlay (base 5s + 0.055/char, cap 20)
- `fdf07a0`+`b24705c` feat(D1): Saweria WS listener (handshake live-verified)
- `5db7f6b` feat(D2): Streamlabs Socket.IO (handshake live-verified; anti-dobel superchat)
- `080c630` feat(E): video watcher (Gemini server-side 2,6s; E2E 4,5s)
- `35ad72e` fix(E): ronde-2 (video always_queue; URL dibuang dari pesan; hold +alert)
- `faa5d67` fix(E): ronde-3 FUNGSIONAL (submit dulu baru hold; ditolak -> fallback thanks)

## Fitur & file inti

| Fitur | File utama | Status live |
|---|---|---|
| A Inisiatif | arti_curious.py (should_fire_initiative, build_initiative_prompt), bridge blok loop | v1 terverifikasi live; SPEK FINAL belum |
| B Recency | arti_vault_rag (_recency_score_boost, is_temporal_query), bridge build_today_block | terverifikasi live |
| C Web lookup | arti_web_lookup.py, pipeline _load_web_lookup | terverifikasi live (bitcoin) |
| D0 SuperChat | bridge extract_superchat/process_message/schedule_donation_trigger | BELUM (butuh SC asli) |
| D1 Saweria | arti_donations.py SaweriaListener | handshake ✔; donasi asli BELUM |
| D2 Streamlabs | arti_donations.py StreamlabsListener | handshake ✔; event asli BELUM |
| E Video | arti_video_watcher.py + wiring bridge | E2E console ✔ (4,5s); live BELUM |
| Nickname/rant/CJK/lock-starvation/VTS-reconnect | tersebar | sebagian live ✔ |

## Spek final inisiatif (2× direvisi Bohan — JANGAN mundur)

Fire bila SEMUA: (1) 30 dtk sejak ARTI terakhir bicara (`_last_arti_reply_ts`);
(2) 5 dtk sejak streamer bersuara APAPUN termasuk pasif (`_last_streamer_speech_ts`);
(3) tidak tts/busy/ptt; (4) `time.time() >= _media_playback_until`.
Chat penonton TIDAK ngeblok. Cadence FLAT (backoff_base=0 di config_local);
eskalasi hanya bila base>0. SIAP = Arti "baru bicara" (masa tenang 30 dtk).

## Alur media share (paling baru, paling rawan)

1. Event masuk (Saweria WS `media.id`+start/end; Streamlabs parser DEFENSIF
   `parse_streamlabs_mediashare` — docs resmi TIDAK mendokumentasikan; payload
   tak dikenal terekam ke `data/streamlabs_events_sample.jsonl`).
2. `_on_donation`: kind media_points (Streamlabs) = kasual tanpa thanks; Saweria
   = thanks digabung ke reaksi video.
3. `_submit_media_job`: submit DULU -> diterima baru `hold_media_playback`
   (alert 5s + klip + 3s); DITOLAK -> return False -> fallback
   `schedule_donation_trigger` (thanks normal). points ditolak = diam.
4. Hold: potong TTS (sd.stop + request_asr_stream_restart), main loop skip
   konsumsi trigger (`continue` SETELAH blok curious/inisiatif; sleep 0.1 di
   atas loop jadi tidak busy-spin), inisiatif ditahan.
5. Watcher `_process`: metadata yt-dlp -> Gemini URL (server-side) -> transkrip
   json3 -> metadata-saja; tulis vault/watch-parties/video-<id>.md; reindex;
   tunggu playback_end; reaksi trigger "video" (always_queue, tak di-drop);
   Q&A window watch-party 300s auto-lepas.
6. Console: `video <url>` / `video skip` (juga nol-kan hold) / `video off|on`.

## Kebijakan terkunci (test menjaga)

- Model: HANYA composer-2.5 (voice/vision) & grok-4.5 effort=high (scout) /
  low (lookup), SEMUA fast=false. Lookup = satu-satunya role boleh tool (web).
- Default shipped semua fitur baru OFF; ON via config_local (gitignored).
- Prioritas queue: donation > yt_chat > video > mic > curious.
- Anti-dobel: superchat relay Streamlabs DIABAIKAN (D0 nangkep dari innertube).

## config_local saat ini (kunci yang ON)

cursor_agent_enabled, cursor_trigger_types [yt_chat,curious,donation,video],
initiative_enabled + initiative_backoff_base_sec 0, web_lookup_enabled,
donation_enabled, video_enabled, supertonic_use_cuda, chain scouter/observer
cursor-first, vision chain nvidia-first lalu cursor.

## Prosedur wajib (pelajaran mahal)

- pytest: TANGKAP RC (`> f; RC=$?; if RC!=0 -> STOP`) — `| tail` menelan exit
  code, 2× kejadian commit saat merah.
- Baseline: critical_error_paths.json (git-tracked; rm -> test regen -> diff
  HANYA entry yang disengaja), module_constants_snapshot.json (gitignored;
  cp ke TMP -> rm -> regen -> diff keys). Konstanta modul bernilai runtime
  HARUS init 0.0 (snapshot merekam VALUE).
- Test dilarang bergantung env mesin (delenv SAWERIA/STREAMLABS di gating).
- Jalur kritis WAJIB diuji FUNGSIONAL (ronde-3: bug urutan hold/submit lolos
  2× review string).
- Private-data grep sebelum commit (vault/, ARTI_*.md, config_local, .env).

## Known unknowns (hanya bisa dibuktikan live)

1. Bentuk payload media share Streamlabs (perekam siap).
2. Perilaku interupsi TTS + hold di kondisi live nyata (timing alert overlay).
3. D0 Super Chat asli; donasi Saweria asli end-to-end (alert -> thanks).
4. Rant mode belum pernah kena dadu live; spek final inisiatif belum disaksikan.
5. Kuota Gemini video di pemakaian ramai (free tier).

## Checklist live (untuk Bohan)

Startup: pemanas scout+vision ok, Index sinkron, Saweria+Streamlabs tersambung,
[Video] Watcher aktif. Uji: diem 30 dtk (inisiatif), `yt harga bitcoin ...`
(lookup), `video <url>` (E manual), media share streamlabs asli (kalau Arti
bengong -> kirim data/streamlabs_events_sample.jsonl), superchat/donasi asli
bila ada, shutdown tunggu banner.
